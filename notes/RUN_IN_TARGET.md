# RUN-IN-TARGET — the heavy-cell manifest

*Generated by `tools/gen_run_in_target.py` — do not edit by hand; regenerate with
`uv run python tools/gen_run_in_target.py`. The scanner shares its regex with
`tools/honesty_audit.py` (NB17 §8 promoted to a repo tool), so the row count below
reconciles exactly with the audit's RUN-IN-TARGET total.*

**The tier rail.** A `# [RUN-IN-TARGET mac|node]` cell is real tier-gated code, and
the tier only ever arrives through config (`run.tier`): the definitive run is the
SAME cell at `tier=target` (or `mid`) — one code path, no forked heavy-run logic
anywhere (PLAN §2). At smoke tier, where the heavy run does not fit, the cell
prints a placard saying what it computes and where its real run belongs — instead
of pretending.

**Where the heavy runs go (hardware locked at sign-off — BUILD_STATE).** The policy
is **mac-first**: every cell that fits the Mac runs there. `mac` = the lab's M2 Max,
96 GB unified memory, MPS (tier `mid`). `node` = ONE 4×A100 80 GB / 900 GB RAM /
90-core machine (tier `target`) — **the hard ceiling**: nothing in the lab may
exceed this single node, and there are no cluster code paths. A `mac|node` label
marks a ladder cell with rungs on both machines (mid on the mac, target on the
node). Smoke — the 4-CPU/15 GB container — runs the whole series live at 1e5–1e6;
`analytical` is 1e8–1e9 extrapolation only.

**Accounting.** 31 rows — one per match of the shared placard regex over
the executed notebooks' code cells, equal to the honesty audit's RUN-IN-TARGET
total (31, re-verified at generation time): 27 genuine
placard cells (12 `mac`, 1 `mac|node`, 14 `node`) plus 4 in-code textual references
to the placard convention (strings inside cells that document or annotate it),
which the shared regex counts identically and this manifest therefore keeps —
marked below — rather than drops. Descriptions are each placard's first comment
line, verbatim (wrapped placards break at the source line).

| Notebook | Label | Section | Placard (first comment line) |
|---|---|---|---|
| `03_is_the_truth_true.ipynb` | `node` | ⚠ This is NOT the gate. The statewide target-tier run is the gate. | definitive statewide MET-05 gate — this cell's real run is tier=target. |
| `04_auditing_real_noise.ipynb` | `node` | Small groups, small power — what these multipliers may claim | definitive statewide NSE-01 audit + exposure fit — real run is tier=target. |
| `05_calibrated_dirt_machine.ipynb` | `mac` | The optimism demo — tune on train, score on eval, one scheme at a time | definitive MET-07: multi-seed split-optimism at tier=mid on the mac — |
| `06_baseline_to_beat.ipynb` | `mac` | 5. Operating points: the protocol asks every system the same question | definitive BAS-01: rerun THESE cells at tier=mid on the mac — the |
| `07_chain_merge_catastrophe.ipynb` | `mac` | 6. The registered demo, drawn | definitive chain-merge numbers: rerun THESE cells at tier=mid on the |
| `08_person_as_a_vector.ipynb` | `node` | 5. Who won — under the lab's protocol, not a leaderboard's | definitive TRN-05: the full 8-cell factorial x run.seeds at the |
| `08_person_as_a_vector.ipynb` | `mac` | 5. Who won — under the lab's protocol, not a leaderboard's | pretrained-subword spot-check (TRN-04's regime factor grazing TRN-05): |
| `09_losses_and_negatives.ipynb` | `mac` | 3c. Decompose, then size — the power table | definitive MET-04: rerun THESE cells at tier=mid on the mac with the |
| `09_losses_and_negatives.ipynb` | `mac` | 4a. Four losses, one budget, one recipe | the OTHER encoder regime: TRN-01's locked factor set is loss x |
| `09_losses_and_negatives.ipynb` | `node` | 4a. Four losses, one budget, one recipe | definitive TRN-01: the locked 8-cell factorial (4 losses x 2 |
| `09_losses_and_negatives.ipynb` | `node` | 5b. The deployable filter — oracle vs soundex, demonstrated | the definitive TRN-02 grid runs the proxy-filter arm at every |
| `09_losses_and_negatives.ipynb` | `node` | 5c. The dial: contamination as a function of residual duplication | definitive TRN-02: the locked 12-cell grid (4 miners x 3 residual- |
| `10_typos_tokenizer_or_augmentation.ipynb` | `node` | 4. The eval-side dial — dose-response on the cheap | definitive TRN-03: the locked 6-cell grid (3 augmentation levels x |
| `10_typos_tokenizer_or_augmentation.ipynb` | `mac` | 5a. The scratch side, live — from TRN-03's winning arm | the pretrained-subword side of TRN-04. huggingface.co is blocked |
| `10_typos_tokenizer_or_augmentation.ipynb` | `mac` | 5a. The scratch side, live — from TRN-03's winning arm | *(in-code reference: the shared audit regex matches this text inside the cell's source; not itself a tier-gated placard cell)* |
| `10_typos_tokenizer_or_augmentation.ipynb` | `mac` | 5a. The scratch side, live — from TRN-03's winning arm | *(in-code reference: the shared audit regex matches this text inside the cell's source; not itself a tier-gated placard cell)* |
| `10_typos_tokenizer_or_augmentation.ipynb` | `mac` | 5d. TRN-04 verdict — PARTIAL at smoke, and that is the point | *(in-code reference: the shared audit regex matches this text inside the cell's source; not itself a tier-gated placard cell)* |
| `11_nicknames_missing_circular.ipynb` | `mac` | 3b. Two budget-matched arms, fully instrumented | the OTHER encoder regime: TRN-06's locked factor set is |
| `11_nicknames_missing_circular.ipynb` | `mac` | 4b. Two budget-matched cells x two eval regimes | definitive missing-stress: the full 8-cell TRN-05 factorial |
| `11_nicknames_missing_circular.ipynb` | `node` | 5. One matrix, two verdicts — through the MET-04 lens | definitive TRN-06: the locked 2x2 (supervision x regime) x |
| `11_nicknames_missing_circular.ipynb` | `node` | 6c. Where does the loss land? The teacher-agreement strata | definitive LABEL-PROV: (a) multi-seed x multi-teacher-draw grid |
| `12_candidates_in_ten_million.ipynb` | `node` | 8. Pricing the real thing: the 1e7 frontier | the definitive BAS-02 frontier: THESE same cells at tier=target on |
| `13_from_pairs_to_people.ipynb` | `node` | 11. Pricing the real thing: CLU-01 at 1e7 | the definitive CLU-01 run: THESE same cells at tier=target on the |
| `14_where_rules_earn_their_place.ipynb` | `mac` | 11. Pricing the definitive runs | definitive HYB-01/NSE-03: THESE same cells at tier=mid on the mac — |
| `15_last_two_orders_of_magnitude.ipynb` | `mac\|node` | 5. The fits — and whether they earned their extrapolation | the definitive SCL-01/02 ladder: THESE same cells at tier=mid on |
| `16_unequal_noise_unequal_errors.ipynb` | `node` | 10. Pricing the definitive run | definitive FAIR-01: THESE same cells at tier=target on the node — |
| `17_verdict_at_1e7.ipynb` | `node` | 7. The 1e7 in the title — measured curves, extrapolated numbers | the definitive Verdict at 1e7: THESE same cells at tier=target on |
| `17_verdict_at_1e7.ipynb` | `<label>` | 8. The honesty audit | *(in-code reference: the shared audit regex matches this text inside the cell's source; not itself a tier-gated placard cell)* |
| `18_your_data_your_schema.ipynb` | `node` | 6f. The cross-STATE arm — priced from this run's coefficients | NC<->OH cross-state linkage: the same §6 cells at tier=target — the |
| `A_appendix_exploratory_arms.ipynb` | `mac` | The tiny cross-encoder — NB12 budget class, stated | the pretrained cross-encoder arm: HF weights are blocked in this |
| `A_appendix_exploratory_arms.ipynb` | `mac` | 7. Closing — what each arm would need to graduate — EXPLORATORY | the definitive appendix arms: THESE same cells at tier=mid on the mac |
